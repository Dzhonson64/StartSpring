<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:musicPlayer.properties"/>
    <bean id="musicBean"
          class="ru.kirill.springcourse.ClassicalMusic"
            init-method="doMyInit"
            destroy-method="doMyDestroy"
            factory-method="getFactoryClassicalMusic">
        <!--
        Добавили методы, который вызывают в начале и в конце жини объекта.

        - модификаторы доступа могут быть любыми
        - любой возвращаемый тип
        - любое название методов
        - НЕ ДОЛЖНЫ принимать аргументов
        - если scope="prototype" Spring НЕ ВЫЗЫВАЕТ destroy-method

        Добавлен фабричный метод getFactoryClassicalMusic()
         -->

    </bean>
    <bean id="musicPlayer"
          class="ru.kirill.springcourse.MusicPlayer"
            scope="prototype">  <!-- Изменение с Singleton (который по умолч) на Prototype. Теперь возвращаться всегда будет новый объект -->



    </bean>

</beans>