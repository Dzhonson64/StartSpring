<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:musicPlayer.properties"/>
    <bean id="musicBean"
          class="ru.kirill.springcourse.RockMusic">
    </bean>
    <bean id="musicPlayer"
          class="ru.kirill.springcourse.MusicPlayer">
       <property name="music" ref="musicBean"/> <!-- Передаём в cеттер (его название это всегда название метода без "set") MusicPlayer ссылку на musicBean (внедряем зависимость)-->

<!--        <property name="name" value="Some name"/>  Передаём значение "Some name" в поле "name" через сеттер setName -->
<!--        <property name="volume" value="50"/>  Передаём значение "50" в поле "volume" через сеттер setVolume -->
        <property name="name" value="${musicPlayer.name}"/>
        <property name="volume" value="${musicPlayer.volume}"/>

    </bean>

</beans>